#include<bits/stdc++.h>
using namespace std;

int main()
{
    long long n,m,i,j,k,l,x,y;
    int M[100];
    while(cin>>n>>m)
    {
        for(i=0;i<m;i++)
        {
            cin>>M[i];
        }
        for(i=0;i<m;i++)
        {
            k=M[i];
            for(j=)
        }
    }
}
